using System;

namespace FormulaDeBascara
{
    class Program
    {
        static void Main(string[] args)
        {

            
            bool delta = Math.Pow(b, 2.0) - 4 * a * c;

            bool x1 = (-b + Math.Sqrt(delta)) / (2.0 * a);
            bool x2 = (-b + Math.Sqrt(delta)) / (2.0 * a);


            Console.WriteLine("Hello World!");
        }
    }
}
